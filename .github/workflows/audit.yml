name: Security audit
on:
    workflow_dispatch:
    schedule:
        - cron: "52 4 10 * *"
          timezone: "Europe/Paris"

jobs:
    audit:
        uses: v4ldum/shared-workflows/.github/workflows/audit-rust.yml@main
