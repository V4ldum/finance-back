[package]
name = "sqlite3_unaccent"
version = "0.1.0"
edition = "2024"

[workspace]
# Excluded from wrapping workspace

[lib]
crate-type = ["cdylib"]

[dependencies]
rusqlite = { version = "0.39.0", features = ["loadable_extension", "functions"] }
deunicode = "1.6.2"
